{
  "name": "ahtml-cloudflare-worker-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker example: serve AHTML snapshots from the edge with KV caching and rate limiting.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@ahtmljs/hono": "0.9.5",
    "@ahtmljs/kv": "0.9.5",
    "@ahtmljs/schema": "0.9.5",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.0.0"
  }
}
